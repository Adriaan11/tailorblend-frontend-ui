# TailorBlend AI Consultant - Frontend UI Environment Configuration
# Note: Blazor uses appsettings.json and appsettings.Development.json for configuration
# This file is for reference only - actual config is in appsettings files

# ============================================================================
# Production Configuration (appsettings.json)
# ============================================================================
# Backend API URL
PythonApi__BaseUrl=https://api.tailorblend.com

# OAuth Configuration (if enabled)
OAuth__Authority=https://tailorblend-oauth.fly.dev
OAuth__ClientId=blazor-consultant
OAuth__ClientSecret=your-oauth-secret

# ============================================================================
# Development Configuration (appsettings.Development.json)
# ============================================================================
# Local backend API
PythonApi__BaseUrl=http://localhost:5000

# OAuth Configuration (development)
OAuth__Authority=https://tailorblend-oauth.fly.dev
OAuth__ClientId=blazor-consultant
OAuth__ClientSecret=61949b676d62a2c3

# ============================================================================
# Runtime Environment Variables
# ============================================================================
# ASP.NET Core environment
ASPNETCORE_ENVIRONMENT=Production
ASPNETCORE_URLS=http://0.0.0.0:8080

# ============================================================================
# Notes
# ============================================================================
# Configuration Priority:
#   1. appsettings.Development.json (local dev)
#   2. appsettings.json (production)
#   3. Environment variables (overrides)
#
# Local Development:
#   - Run with: dotnet run --project BlazorConsultant
#   - Frontend on: http://localhost:8080
#   - Backend on: http://localhost:5000
#   - Ensure Python backend is running!
#
# Production Deployment:
#   - Set PythonApi__BaseUrl to backend API URL
#   - Use fly secrets or Railway env vars
#   - HTTPS enforced
#   - CORS restricted to authorized origins
